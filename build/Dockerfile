# This is mostly a placeholder image, so that nanobox can depend
# on this image's existence now. As customizations are needed,
# they can be added here without changing external systems.
FROM gliderlabs/herokuish

# ENV USER=herokuish
